// All material is licensed under the GNU Free Documentation License
// https://github.com/ArdanStudios/gotraining/blob/master/LICENSE

// http://play.golang.org/p/TBZJBNhoO4

// Declare and make a map of integer values with a string as the key. Populate the
// map with five values and iterate over the map to display the key/value pairs.
package main

// main is the entry point for the application.
func main() {
	// Declare and make a map of integer type values.
	/*
		map_name := make(map[type]type)
	*/

	// Intialize some data into the map.
	/*
		map_name[key] = value
		map_name[key] = value
		map_name[key] = value
		map_name[key] = value
		map_name[key] = value
	*/

	// Display each key/value pair.
	/*
		for variable_name, variable_name := range map_name {
			fmt.Printf("Dept: %s People: %d\n", variable_name, variable_name)
		}
	*/
}
